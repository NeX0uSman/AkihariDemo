import Header from "../components/Header";
import Hero from "../components/Hero";

const page = () => {
  return (
    <main className="font(--font-nunito)">
      <Header />
      <Hero />
    </main>
  );
};

export default page;
